# Empty means deploy the image pinned in values.yaml.
# Set both to build a ref that has no published image. Use a commit sha or a tag,
# because the ref becomes the image tag and a branch name would never rebuild.
source:
  repo: ""
  ref: ""
